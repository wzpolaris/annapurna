make an spa 

use vite, react

page should have two buttons: "Upload" and "View"

Upload uploads a file from local computer. After a file is loaded, the web page should show the filename and size near the button.

After a file is loaded, if View is pressed, if the file extension is .pdf, then a pdf viewer opens.



